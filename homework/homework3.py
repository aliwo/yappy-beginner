# TODO 1. PY.CHECKIO 문제 풀어오기
# while이랑 if문을 완전 제 마음대로 넣은 거 같은데요..!?
def password(x):
    x = input() as 'string'

    while len(x) >= 10:
        if str.islower(x)=false:
        elif str.isupper(x)=false:
        else str.isdigit(x)=false:

    return print("congrats! safe password")


#TODO 2. 구구단 역순으로 출력하기
for i in reversed (range(1,10)):
    for j in reversed (range(1,10)):
       print (i*j)

